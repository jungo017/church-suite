// @church/module-members — 교적(교인) 모듈 (module-platform.md M4).
// 호스트(셸)가 소비하는 공개 표면: 매니페스트(네비/권한/소유스키마 합성).
// 내부 구현(service/actions/attendance/org/portal 등)은 서브패스로 접근.
export { membersManifest } from "./manifest";
